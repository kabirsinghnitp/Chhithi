<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chitthi</title>
    <link rel="stylesheet" href="/css/styles.min.css">
    <link rel="icon" href="/img/letters.png">
    <link rel="manifest" href="/js/pwa.webmanifest">
</head>
<body>
    <div class="centered-form">
        <div class="centered-form__box">
            <form action="/chat/<%= one %>/<%= id%>" method="GET" >
                <input style="display: none;" type="text" name="one" placeholder="one" value="<%= one %>" required>
                <input style="display: none;" type="text" name="two" placeholder="two" value="<%= two %>" required>
                <button>Connect</button>
            </form>
            <form action="/logout" method="POST">
                <button>Logout</button>
            </form>
        </div>
    </div>
    <script src="/socket.io/socket.io.js"></script>
</body>
</html>
